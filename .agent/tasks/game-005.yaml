task:
  id: GAME-005
  title: Notification DLQ exercise
  status: DONE
  objective: Verify staging retry exhaustion routes an isolated failed notification reference to the DLQ.
  depends_on: [QUEUE-005]
  scope:
    include:
      [
        .agent/status.yaml,
        .agent/tasks/game-005.yaml,
        workers/gateway/wrangler.jsonc,
        docs/evidence/game-005/2026-09-07.md,
      ]
    exclude: [production queues, provider delivery, user data]
  validation:
    commands: [wrangler queues list, private staging publisher invocation]
  rollback:
    strategy: Remove only the approved staging queues after the exercise if no longer needed.
