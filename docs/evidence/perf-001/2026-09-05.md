# PERF-001 Evidence

- Owner approved an isolated staging deployment. Wrangler deployed `atmos-gateway-staging` as version `a71511c0-a247-4396-a334-7b7571f1ff7a` with no inherited Queue binding.
- The staging Worker is isolated from production and has no configured route. Its `workers.dev` subdomain was registered after deployment.
- CPU p95 is not yet recorded: repeated smoke requests are blocked during Workers.dev TLS propagation with `ERR_SSL_VERSION_OR_CIPHER_MISMATCH`.
- No production Worker, production route, queue, database, secret, or paid resource was changed.
