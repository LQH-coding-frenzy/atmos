# PERF-001 Evidence

- Owner approved an isolated staging deployment. Wrangler deployed `atmos-gateway-staging` as version `a71511c0-a247-4396-a334-7b7571f1ff7a` with no inherited Queue binding.
- The staging Worker is isolated from production and has no configured route. Its `workers.dev` subdomain is now reachable.
- The provider failure was fixed by binding the default Open-Meteo fetcher to `globalThis`, preserving Cloudflare Workers' required `this` reference. Staging dashboard requests now return `200`, and consecutive requests return `x-cache: HIT`.
- Cloudflare Analytics GraphQL measured a 111-request warmed-cache workload starting at `2026-09-05T16:19:45Z`: 99 explicit cache hits from the controlled 100-request sample, zero errors, CPU p50 `0.728 ms`, p95 `4.731 ms`, and p99 `6.716 ms`. The p95 is below the 7 ms Worker budget.
- A prior mixed miss/failure window recorded p95 `7.801 ms`; it is retained as diagnostic context and is not used as budget evidence for the warmed public-read workload.
- No production Worker, production route, queue, database, secret, or paid resource was changed.
