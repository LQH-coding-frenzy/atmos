# CTR-002 Evidence

## Scope And Remediation

- Exact dependency `CTR-001` was `DONE` before implementation. CTR-002 changed only the job image, protected container workflow, local tool pins, supply-chain validation, documentation, and task record. It created no Azure resource, database change, secret, signing key, or paid resource.
- The initial local exact-image Trivy policy scan correctly rejected the CTR-001 base because its bundled npm `tar` dependency had a critical finding. A newer official Node base alone still had fixed high findings in its Alpine OpenSSL libraries and unused package-manager dependencies. No exception, severity reduction, `ignore-unfixed`, or scan suppression was introduced.
- The runtime now pins `node:24.21.0-alpine3.23@sha256:159fe64649038c30f8cc1ec4be3af3a6e93e3648678c31294e2c5058dbeb99f3`. Its one deterministic remediation layer upgrades only `libcrypto3` and `libssl3` to `3.5.8-r0` and removes unused npm/corepack tooling. It still runs only the finite probe as `node:node` without runtime package management.

## Local Validation

- Focused container policy tests passed, and the full local suite passed 56 Vitest and 45 Node tests. Lint, TypeScript typecheck, scoped Prettier checks, and `git diff --check` passed. Prettier has no parser for Dockerfiles or `mise.toml`; those files were reviewed directly.
- Trivy v0.74.0 scanned the rebuilt local image with `vuln,secret,misconfig`, `HIGH,CRITICAL`, and nonzero exit-on-finding policy. The final scan reported zero vulnerabilities, secrets, and misconfigurations at that threshold.
- Trivy generated a CycloneDX SBOM for the final local image. The structural validator accepted its container root and 20 components.
- The local image completed successfully under `--read-only`, `--network=none`, `--cap-drop=ALL`, and `no-new-privileges`, emitting only the expected finite lifecycle events.
- The configured reviewer and security-reviewer agents were unavailable because their configured model could not be resolved. Manual review confirmed least-privilege permissions, full-SHA action pins, pre-login scan ordering, no scanner bypass, no long-lived credential, exact digest targeting, config-digest equality, and a retained rollback path.

## Protected Validation And Publication

- PR #146 passed all protected checks. Its non-publishing `Container release` validation run `34808433285`, job `103864799631`, built the image, enforced the Trivy policy, generated and validated the SBOM, and exercised the hardened runtime with only `contents: read`.
- Protected merge commit `089f782bec7f2e11ad9aab5f402af3fe30e4b2ef` triggered `Container release` run `34808618659`. Validation job `103865333479` and protected publish job `103865507585` both succeeded.
- The publish job built and scanned the exact tagged image before GHCR login, generated and retained the CycloneDX SBOM artifact `atmos-jobs-sbom-089f782bec7f2e11ad9aab5f402af3fe30e4b2ef` (artifact ID `10333339455`, 3,264 bytes), pushed it, and confirmed the remote manifest config digest equals the config digest scanned locally.
- The same protected job used only the ephemeral `GITHUB_TOKEN` for package publishing and GitHub OIDC for Cosign v3.0.6. Keyless `cosign sign` and `cosign attest --type cyclonedx` both succeeded for the exact digest; no personal token or signing key was created.
- Exact tag: `ghcr.io/lqh-coding-frenzy/atmos-jobs:sha-089f782bec7f2e11ad9aab5f402af3fe30e4b2ef`.
- Exact digest: `sha256:3d30bb6c88ba84e46e0ff592ebf3c0870bb207c59e7e6feb48902c41350e0975`.

## Independent Retrieval And Boundaries

- An empty Docker credential directory anonymously pulled the exact digest. Remote inspection reported source `https://github.com/LQH-coding-frenzy/atmos`, revision `089f782bec7f2e11ad9aab5f402af3fe30e4b2ef`, user `node:node`, exec-form entrypoint `["node","/app/job.mjs"]`, and local size 61,073,757 bytes.
- The anonymously pulled exact digest completed under the locked-down runtime flags with release `089f782bec7f` and expected lifecycle events.
- CTR-003 remains responsible for verification of the expected GitHub Actions identity, Cosign signature, and CycloneDX attestation on the exact digest before any Azure deployment. No image is yet approved for Azure and no Azure action was taken.
- Retain the immutable signed digest and SBOM artifact. Recover only by selecting a retained digest that later passes CTR-003 verification; never overwrite a SHA tag, deploy by a mutable tag, or suppress a security finding.
