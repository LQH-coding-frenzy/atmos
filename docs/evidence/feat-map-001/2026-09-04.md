# FEAT-MAP-001 Evidence

- Mock-map test: 1 passing. It verifies the MapLibre GeoJSON point and map center preserve the normalized dashboard coordinates without creating a synthetic location.
- `corepack pnpm --filter @atmos/web build`, `pnpm lint`, and `pnpm typecheck` pass.
- The dashboard map uses pinned `maplibre-gl@6.7.0`, an inline dark style, local GeoJSON, browser navigation controls, resize handling, and removal cleanup.
- `apps/web/lib/mock-map.ts` explicitly traces the current `MockWeatherProvider` dashboard boundary. No tile URL, map credential, place search, live weather overlay, or user-location read exists in this task.
- No provider request, database migration, remote deployment, or paid-resource change occurred.
