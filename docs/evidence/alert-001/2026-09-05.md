# ALERT-001 Evidence

- `corepack pnpm exec supabase db reset` reconstructs the local schema through `20260905010000_alert_rules.sql`.
- `corepack pnpm test:db` passes 54 pgTAP assertions across baseline, snapshot, and alert-rule suites.
- Alert-rule coverage verifies bounded rule columns, explicit authenticated CRUD grants, enabled RLS, owner CRUD allow behavior, and cross-user select/insert/update/delete denial.
- No existing policy was changed or weakened. No evaluator, schedule, notification, remote database apply, secret-key use, or paid-resource change occurred.
