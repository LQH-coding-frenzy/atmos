# PREVIEW-001 Evidence

- Vercel project `atmos` is connected to `LQH-coding-frenzy/atmos` with `apps/web` as its Root Directory.
- The first Git preview exposed a monorepo defect: workspace package declarations were not built before Next.js typechecking.
- Git-owned Vercel configuration now performs a frozen pnpm install and builds contracts, domain, provider, and web packages in dependency order.
- `corepack pnpm dlx vercel build --yes --scope 23520608-3283s-projects` completed successfully and produced the local Vercel output bundle.
- The protected preview status remains required before finalization.
