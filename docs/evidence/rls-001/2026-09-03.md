# RLS-001 Evidence

## Local Validation

- `corepack pnpm exec supabase db reset`
- `corepack pnpm test:db` with 20 passing pgTAP assertions

## Coverage

Fixtures create two authenticated users through the real Auth trigger. The tests verify that User A can select own `profiles` and `user_preferences` rows and cannot select, update, or delete User B rows.

## Rollback

Revert the test-only coverage commit. It does not change migrations, deployed policies, or production data.
