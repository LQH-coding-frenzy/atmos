# FUNC-002 Evidence

- `GET /api/v1/me` requires an authenticated bearer token, derives the caller through Supabase Auth, and reads `profiles` and `user_preferences` with the caller-scoped client.
- The endpoint returns sanitized `401` and `503` errors with a request ID; it does not use the service-role key.
- `corepack pnpm exec supabase functions serve api-v1 --no-verify-jwt` loaded `api-v1` successfully at `http://127.0.0.1:54321/functions/v1/api-v1`.
- No schema, remote deployment, or privileged credential change occurred.
