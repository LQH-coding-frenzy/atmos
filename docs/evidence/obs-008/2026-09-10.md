# OBS-008 Evidence

- Dependency `OBS-007` is `DONE`; staging dataset `atmos_worker_requests_staging` is active.
- The owner approved a new least-privilege Analytics read credential. User token `atmos-staging-wae-sql-read-user` has only `Account Analytics Read`, applies only to the Atmos Cloudflare account, and expires December 12, 2026.
- Cloudflare's user-token copy control produced a `curl` command. The bearer field was extracted in memory, verified against `SHOW TABLES`, moved directly into GitHub environment secret `CLOUDFLARE_ANALYTICS_TOKEN`, and cleared from the clipboard without printing or writing it to the workspace.
- GitHub environment `Staging` permits protected branches only. Non-secret `CLOUDFLARE_ACCOUNT_ID` is an environment variable.
- The workflow has `contents: read`, uses only full-SHA actions, rejects non-main refs, and has no provider write/deploy operation.
- SQL identifiers and numeric bounds are validated before interpolation. Queries use `_sample_interval` for weighted counts/error rates and `quantileExactWeighted` for p95.
- Six focused tests pass across weighted SQL generation, PASS, both FAIL paths, INSUFFICIENT_DATA, identity/metric rejection, response parsing, endpoint authentication, and sanitized API errors.
- Repository typecheck and lint pass; owned files pass formatting and diff checks.
- Configured reviewer and security-reviewer agents were unavailable because their model could not be resolved; manual review moved dispatch inputs through environment variables, bounded error/latency metrics, added a 30-second API timeout, and reconfirmed read-only least privilege.
- Protected implementation PR #110 passed Vercel, quality, database, Playwright, SBOM, CodeQL, Gitleaks, Semgrep, and Trivy checks before merge as `14434e26b79b071e6098d3b5ce69f885fd89d153`.
- Exact candidate Worker version `e727993c-fad4-4caf-94e8-53aacc56eb48` was preview-smoked and deployed only to staging; it reports release `14434e26b79b`, while stable version `84711379-0d86-4e62-886c-68d5fe2c0c18` / release `b67be04c5f67` remains available for rollback and comparison.
- Protected workflow run `34556676956` confirmed the Staging environment injected both masked values, but the SQL API returned HTTP 400. The identical generated SQL returned rows in Cloudflare query studio, isolating the failure to API authentication/transport rather than query syntax.
- The correction reports only numeric Cloudflare error codes on non-2xx responses and discards provider messages/bodies, preserving diagnostic value without leaking credentials.
- Correction PR #111 passed every protected check and merged as `bcd95618950bc1f0a459e64996fb8c23e4bff8bf`. Protected run `34565736795` then identified Cloudflare code `9106` without exposing a provider message.
- Two account-owned tokens with the required permission both returned `9106` and showed no last use. After the scoped user token succeeded with HTTP 200, both unused account-owned tokens were deleted.
- Protected run `34566875185` returned `INSUFFICIENT_DATA`: stable had 506 weighted samples and candidate had 20, both with zero errors. Fifteen bounded staging weather requests all returned 200; the minimum sample threshold was not reduced.
- Protected run `34567204458` returned `PASS`: stable had 506 weighted samples, zero errors, and p95 wall duration 1,032 ms; candidate had 35 weighted samples, zero errors, and p95 wall duration 867 ms. The candidate stayed within the 2% error-rate and 2,500 ms p95 limits.
