# WX-004 Evidence

## Provider Contract

Open-Meteo's official terms and pricing pages were rechecked on 2026-09-03. The Free API remains non-commercial and limited to 600 calls per minute, 5,000 per hour, 10,000 per day, and 300,000 per month. Weather data requires CC BY 4.0 attribution.

## Guard Scope

`OpenMeteoUsageGuard` provides deterministic process-local warnings at 70 percent and rejects calls that would exceed a documented limit. Its monthly count resets at the UTC calendar-month boundary. It is not a distributed quota counter. `EDGE-004` and `CACHE-001` must add the cache/coalescing and centralized observability required before public traffic.

## Local Validation

- `corepack pnpm exec vitest run packages/provider-openmeteo/src/index.test.ts` with 5 passing provider tests
- `corepack pnpm --filter @atmos/provider-openmeteo build`
- `git diff --check`

## Rollback

Revert the WX-004 commit before any public route is deployed. No remote state, provider configuration, or paid resource is changed.
