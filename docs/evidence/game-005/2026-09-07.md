# GAME-005 Evidence

- Approved isolated staging queues `atmos-notifications` and `atmos-notifications-dlq` were created.
- Staging Worker version `f7111819-67e2-47d9-85d8-b400c11e9083` has the primary queue producer and consumer binding, maximum three retries, and the DLQ target.
- The matching `INTERNAL_QUEUE_SECRET` is configured in staging Worker and Supabase Edge Function secret stores; values were not displayed or committed.
- The private publisher accepted one intentionally malformed reference. Its durable claim must fail, causing retry exhaustion without accessing user data.
- Wrangler queue inspection confirms primary and DLQ resources/bindings. Cloudflare Dashboard DLQ metrics recorded one billable operation after the failed message, confirming DLQ arrival without consuming the retained message.
- Protected PR #57 passed quality, database, Playwright, CodeQL, Gitleaks, Semgrep, and Trivy checks before merge.
