# REL-006 Evidence

- Dependency `REL-004` is `DONE`. The owner approved a least-privilege Cloudflare Worker deployment token and protected GitHub storage on 2026-09-13.
- GitHub environment `Production` contains secret name `CLOUDFLARE_API_TOKEN` and non-secret variable `CLOUDFLARE_ACCOUNT_ID`. No credential value was printed, read, or committed.

## Protected implementation

- PR #132 merged the rollback workflow and validation code as `78b55ce6dfcf3378a049b237f73d61891d0eded5` after quality, database, Playwright, CodeQL, Gitleaks, Semgrep, Trivy, SBOM, and Vercel checks passed.
- `.github/workflows/worker-rollback.yml` runs only from protected `main`, uses environment `Production`, has `contents: read`, serializes rollback runs, and pins both third-party actions to full commit SHAs.
- The Cloudflare credential is scoped only to inventory and deployment steps. Inputs are strict Worker UUIDs and 12-character release IDs; arbitrary messages and secrets are not accepted as inputs.
- The workflow verifies Cloudflare version-to-release tags, current failed-version traffic, retained stable deployment history, explicit mode confirmation, and the exact resulting stable-only deployment.
- The retained target is smoke-tested through `Cloudflare-Workers-Version-Overrides` before execute mode can change ordinary traffic. Post-deployment smoke verifies health, release identity, public weather, and unauthenticated HTTP 401 behavior.
- `corepack pnpm test` passed 56 Vitest tests and 26 Node script tests, including six rollback-control tests. Lint, typecheck, gateway dry-run build, runbook validation, formatting, and `git diff --check` passed.

## Live plan validation

- Protected workflow run `34751091484` executed plan mode from exact `main` SHA `78b55ce6dfcf3378a049b237f73d61891d0eded5` and completed successfully.
- The plan mapped retained Worker `335f0f53-c301-4cc3-bd84-2b9b4e799b9b` to release `763e8c651218` and current Worker `57d8764d-c7e2-41d2-9e46-00c74f17f4c6` to release `a794b540e4dc`.
- Cloudflare inventory preconditions and the retained target's override health/version/weather/authorization smoke passed.
- Deploy, post-deployment verification, and recovered-path smoke steps were skipped as required in plan mode.
- Post-run inventory still reports deployment `572dd0e1-6de5-46f9-b2f1-5454aaf3e414`, with current Worker `57d8764d-c7e2-41d2-9e46-00c74f17f4c6` at 100 percent and the retained predecessor at zero. No production traffic changed.
- This predecessor is proof of plan mechanics, not the target for GAME-002. A bad-canary exercise must retain current known-good release `a794b540e4dc` as stable before introducing its test candidate.

## Rollback

- Revert the workflow commit through protected Git if validation logic is defective. No runtime rollback is needed for the plan-only evidence run.
- Execute mode restores only an exact retained Worker version at 100 percent. It preserves additive database schema and all referenced Supabase functions for later recovery and REL-007 cleanup checks.
