# CRON-001 Evidence

- Added the additive coordinate prerequisite for enabled alert rules; existing rules remain compatible because both columns are nullable.
- Local schema reset applies the migration, and all 68 pgTAP assertions pass.
- The staging `api-v1` deployment exposes a secret-gated internal evaluator route. It returns aggregate counts and skips unsupported metrics rather than fabricating weather facts.
- Staging `pg_cron` and `pg_net` are enabled. Active job `atmos-evaluate-alerts-staging` runs every 15 minutes and reads its authorization header from Vault.
- No production scheduler, queue, notification delivery, or production database change occurred.
