# DBREL-001 Staging Promotion Evidence

- The linked target is `atmos-staging` (`btrvybyprlcumtovdxsc`); production project `oxgwprvkotfvyacpqayx` is explicitly excluded.
- Remote history initially matched through `20260906000000` and lacked exactly `20260907000000` and `20260908000000`.
- `supabase db push --linked --dry-run` listed only the reviewed alert-events and saved-locations migrations.
- Both migrations are additive, preserve stable backend behavior, enable RLS, and grant only the reviewed authenticated operations.
- A clean local reset and all 107 pgTAP assertions passed before staging apply.
- Rollback is forward-fix: retain additive relations through the release window rather than dropping staging data.
- `corepack pnpm exec supabase db push --linked` applied both migrations successfully to `atmos-staging` after owner approval.
- The post-apply remote migration list contains all seven local versions through `20260908000000`; no migration remains pending.
- The configured security-reviewer was unavailable because its model could not be resolved; manual review reconfirmed both tables use RLS, explicit authenticated grants, owner-scoped policies, and additive foreign keys/indexes only.
