# REL-007 Evidence

- Exact dependency `REL-006` is `DONE`.
- The owner authorized continued end-to-end production completion on 2026-09-13. No new credential, paid resource, database change, or Worker traffic change was required.

## Protected Implementation

- PR #134 merged the guard as `5703f08d39d7996016a82536a6e14aada34b2963` after quality, database, Playwright, CodeQL, Gitleaks, Semgrep, Trivy, SBOM, and Vercel checks passed.
- The guard obtains fresh Cloudflare deployment/version inventories and the production Supabase function inventory itself. It accepts no project override and invokes deletion only after exact candidate and confirmation checks.
- `api-v1` and every `api-<release>` mapped by every retained Worker's canonical release tag are protected. Cleanup fails closed if provider JSON is malformed, a deployment reference is missing, a protected function is inactive, or fewer than three distinct release functions remain.
- `corepack pnpm test` passed 56 Vitest tests and 34 Node script tests. Lint, typecheck, targeted formatting, and `git diff --check` passed.
- The external security-review subagent was unavailable because its configured model was not found. Protected repository scanners passed and a manual release-safety review closed the initial separate-authorization bypass before merge.

## Production Cleanup

- The pre-cleanup live plan protected `api-v1`, `api-43ba87155532`, `api-763e8c651218`, and `api-a794b540e4dc`.
- The only candidates were `api-1163da002bf4` and `api-536fd3697b60`. Neither release appears in the complete retained Worker version inventory.
- The guard authorized and deleted `api-1163da002bf4`, then recaptured all provider inventories. The next plan protected the same four functions and identified only `api-536fd3697b60`.
- The guard authorized and deleted `api-536fd3697b60`, then recaptured all provider inventories again.
- The final live plan protects the same four functions, retains Worker releases `43ba87155532`, `763e8c651218`, and `a794b540e4dc`, and reports zero cleanup candidates.
- Wrangler documents that `versions list` returns at most 10 versions. Production had only three, so cleanup used the complete current inventory; the guard now blocks cleanup whenever the 10-item limit is reached rather than risking an omitted rollback reference.
- Direct post-cleanup probes return HTTP 404 for both removed function routes and HTTP 200 for all four protected function health routes.
- Production Worker deployment and traffic were not changed. No database data, schema, RLS policy, or secret was changed.

## Rollback

- Both removed release IDs resolve to immutable Git commits. If either unreferenced wrapper is needed again, check out that exact commit, generate its deterministic wrapper with `corepack pnpm release:supabase:stage`, and redeploy the exact function name.
- Immediate Worker rollback remains protected by three active release-mapped functions plus canonical `api-v1`.
