# CTR-001 Evidence

## Scope And Runtime

- Exact dependency `GHCR-001` was `DONE` before implementation.
- The runtime probe uses official `node:24.14.1-alpine3.23` pinned to OCI index digest `sha256:8510330d3eb72c804231a834b1a8ebb55cb3796c3e4431297a24d246b8add4d5`.
- The Dockerfile installs no package, copies only `job.mjs`, runs as `node:node`, uses an exec-form entrypoint, and writes no file.
- The runtime reads only bounded `ATMOS_JOB_ID` and 12-character lowercase hexadecimal `ATMOS_RELEASE_ID`. It performs no network, database, provider, user-data, or durable-state operation.
- Valid configuration emits fixed `job_started` and `job_completed` JSON events. Invalid configuration emits only fixed `job_configuration_invalid` metadata and exits non-zero without reflecting the rejected value.

## Validation And Review

- Six focused runtime and GHCR policy tests passed. The full local suite passed 56 Vitest and 42 Node tests; lint, typecheck, scoped formatting, and `git diff --check` passed.
- Local Docker validation built the digest-pinned image, reported user `node:node` and entrypoint `["node","/app/job.mjs"]`, and completed under `--read-only`, `--network=none`, `--cap-drop=ALL`, and `no-new-privileges`.
- The invalid-release container exercise emitted the fixed failure event and exited non-zero. The valid exercise emitted exactly two lifecycle events and exited zero.
- Initial lint identified that Node globals were scoped only to `scripts/**/*.mjs`. The existing ESLint Node scope was extended to `jobs/**/*.mjs`; no rule was disabled or suppressed.
- The configured security-reviewer was unavailable because its model could not be resolved. Manual review found no credential path, reflected input, mutable base/tag, pull-request package write, writable-filesystem dependency, unbounded loop, or overlap with signing controls.
- PR #144 passed container validation, quality, database, Playwright, CodeQL, Gitleaks, Semgrep, Trivy configuration scanning, serverless SBOM, and Vercel checks before merging as `d115517`.

## Protected Publication

- Protected workflow run `34769422676` executed from exact merge SHA `d115517d3a813a3caebd1fe4c0a810767908aab5`. Validation job `103756166103` and publish job `103756193771` both passed.
- Exact tag: `ghcr.io/lqh-coding-frenzy/atmos-jobs:sha-d115517d3a813a3caebd1fe4c0a810767908aab5`.
- Exact digest: `sha256:1f652bd697206b0470d1770581c497817ff1f7867e1612bb21bdda24401f7d14`.
- An empty Docker credential directory resolved the tag and pulled the exact digest, proving public access without a registry credential.
- Remote inspection reported Linux/amd64, 56,402,251 uncompressed bytes, user `node:node`, the exec-form entrypoint, source `https://github.com/LQH-coding-frenzy/atmos`, and exact revision `d115517d3a813a3caebd1fe4c0a810767908aab5`.
- The anonymously pulled exact digest completed with the hardened runtime flags and emitted only the expected two lifecycle events for job `ctr-001-public` and release `d115517d3a81`.

## Boundaries And Rollback

- Current Trivy protected checks cover the Dockerfile and repository configuration, not the published image filesystem. `CTR-002` remains responsible for image vulnerability policy, SBOM generation, and Cosign signing.
- No Azure resource exists and no image is approved for Azure deployment. `CTR-003` must verify the Cosign identity and signature on the exact digest before deployment.
- The GHCR-001 bootstrap and CTR-001 runtime digests remain retained. Recover by selecting a previously validated digest or reverting through protected Git; never overwrite an existing SHA tag.
