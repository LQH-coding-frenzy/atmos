# CACHE-001 Evidence

- Dashboard weather responses use the Worker Cache API only after successful provider normalization, with a five-minute public TTL.
- Gateway tests prove a cache hit avoids the provider and a miss stores a successful response; invalid input and provider failures return before cache writes.
- No remote Worker deploy, cache purge, stale-on-error response, provider credential, or paid-resource change occurred.
