# FRONTREL-001 Evidence

- Dependency `PREVIEW-001` is `DONE`; Vercel Hobby project `atmos` remains connected to `LQH-coding-frenzy/atmos` with `apps/web` as its Root Directory.
- The owner approved production execution and an expiring token. A project-only token proved valid against the Atmos Vercel API but Vercel CLI rejected that scope before deployment; the owner then approved the required team scope and the project-only token was deleted.
- The first team token was rotated immediately after browser-generated untracked snapshots captured it. Those files, the first team token, and the expired cached CLI token were deleted; a workspace search confirms no Vercel token remains.
- Active token `atmos-production-deploy-ci-v2` is limited to team `23520608-3283's projects`, expires December 11, 2026, and passed `vercel whoami`. Its value moved directly from the dashboard clipboard into protected GitHub environment secret `VERCEL_TOKEN` without being printed or written to the workspace, and the clipboard was cleared.
- GitHub environment `Production` is restricted to protected branches. `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`, `VERCEL_SCOPE`, and `PRODUCTION_URL` are non-secret environment variables.
- The release workflow is manual, read-only in GitHub, rejects non-main refs, pins Actions and Vercel CLI versions, and scopes `VERCEL_TOKEN` only to steps that call authenticated Vercel APIs.
- Response hardening defines CSP, HSTS, frame denial, MIME sniffing prevention, referrer policy, and browser feature restrictions. PR previews remain enabled while only `main` Git auto-deploy is disabled.
- Three response-verifier tests, two Next configuration tests, the web production build, repository typecheck, and lint pass; owned files pass formatting and diff checks.
- Configured security-reviewer execution was unavailable because its model could not be resolved. Manual review removed the token from dependency-install and unrelated build environments, validates deployment URL origins, captures the prior deployment, and limits rollback to a failed post-promotion smoke.
- Pending protected implementation PR and protected production workflow evidence.
