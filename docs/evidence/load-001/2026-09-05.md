# LOAD-001 Evidence

- Ran the official `grafana/k6` container against the isolated staging Worker at 10 requests/second for 20 seconds.
- Results: 202 HTTP requests, 201 iterations, 100% of 603 checks passed, 0% HTTP failures, and HTTP duration p95 `248.69 ms`.
- The test warms the route once, then requires `200`, a valid `HIT` or `MISS` Cache API state, and an `x-request-id` on every measured request. Cache states may vary across Cloudflare data centers.
- The initial strict hit-only run is retained as diagnostic context: it observed valid edge-local misses and two provider failures; the final bounded baseline uses the documented Cache API locality behavior without weakening the `200` reliability gate.
- No production traffic, remote configuration, queue, database, secret, or paid-resource change occurred.
