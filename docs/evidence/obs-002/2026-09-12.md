# OBS-002 Evidence

- Dependency `OBS-001` is `DONE`. The owner approved Grafana Cloud Free setup with a hard stop before paid upgrade or quota exception on 2026-09-12.
- Grafana's current pricing page identifies Free as always `$0` with no charges. The account plan comparison shows `Cloud Free` as the current plan, states that no credit card is required, and caps usage at Free limits.
- A temporary unlimited onboarding trial ends September 26, 2026. Atmos ignores trial capacity for budgeting and did not add a card or select Cloud Pro.
- Grafana organization and stack slug are `stouttortoise1569`. Stack `1827502` is active at `https://stouttortoise1569.grafana.net` in AWS Singapore (`prod-ap-southeast-1`) with deletion protection enabled.
- Current stack counters observed during preflight were zero active metric series, zero log ingest, and zero trace ingest. No telemetry stream was enabled by this task.
- Created empty Grafana folder `Atmos` with UID `fbdzpj`. The folder page reports zero dashboards, zero panels, and zero alert rules.
- Current published Free allowances include 100,000 API and 10,000 browser synthetic executions per month. Four future API checks at one probe every 15 minutes project to 11,520 executions in a 30-day month when each check finishes within one minute, 11.52 percent of the API allowance. A conservative two-minute assumption is 23,040 executions, or 23.04 percent.
- No access policy, service account, token, synthetic check, data source, payment method, paid plan, or additional stack was created.
- Browser verification confirmed the plan, active stack metadata, delete protection, region, and empty Atmos folder. One provider UI request for absent per-user saved-query storage returned 404 without affecting folder creation or navigation.
- The configured security-review agent was unavailable because its model could not be resolved. Manual review found no credential exposure, paid-resource path, quota breach, or misleading runtime claim. The permanent plan display and caps must be revalidated after the trial ends.
- Rollback is limited to removing the empty Atmos folder. The deletion-protected Free stack remains the dependency target for SLO-001 and supported telemetry tasks.
