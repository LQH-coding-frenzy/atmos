# Provider Preflight 2026-09-21

Result: scoped GO for CVRET-001 retirement only.

- The owner explicitly approved retirement of the queue, alert-delivery, and unused Azure container-platform runtime. The approval excludes Supabase schema/RLS changes, R2 archive deletion, and paid-resource creation.
- `atmos-edge-production` completed an HCP Terraform speculative plan from the reviewed source: `0 to add, 0 to change, 2 to destroy`. Only `cloudflare_queue.notifications` and `cloudflare_queue.notifications_dlq` are scheduled for deletion; the custom Worker domain and `atmos-production-backups` remain in state.
- `atmos-azure-production` completed an HCP Terraform speculative plan from the reviewed source: `0 to add, 0 to change, 2 to destroy`. Only the unused Container Apps environment and Log Analytics workspace are scheduled for deletion; the one-shot backup Container Instance remains in state.
- Azure for Students is enabled with the recorded USD 10 monthly guardrail and Owner-role budget notifications from `docs/evidence/az-001/2026-09-14.md`. Retirement creates no Azure resource or paid tier.
- The protected retirement workflow runs only from `main`, requires the `Production` environment and an exact typed confirmation, deploys/smokes a zero-percent Worker candidate before promotion, then detaches queue triggers before replacing canonical `api-v1` and removing retired secret names.
- Local database/RLS validation, full lint/type/test suite, web build, Worker bundle dry-run, browser tests, runbook validation, Terraform validation, targeted formatting, and diff checks passed. Repository-wide formatting remains blocked only by unrelated tracked `.playwright-mcp` snapshots, which CVRET-001 does not modify.
- Final remote operations remain limited to the approved workflow dispatch and manual HCP Terraform applies after protected merge. Evidence must record final release identities, deletion summaries, and public health checks without secrets or provider response bodies.
