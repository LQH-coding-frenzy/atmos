# Provider Preflight 2026-09-13

Result: scoped GO for GHCR-001; provider-dependent blocked lanes remain blocked.

- GitHub repository `LQH-coding-frenzy/atmos` is public and protected `main` is the default branch. Public package publication is eligible for the portfolio plan; GitHub documents public Packages as free and states that Container Registry image storage and bandwidth are currently free.
- GHCR-001 uses the automatic per-run `GITHUB_TOKEN` with job-scoped `contents: read` and `packages: write`. It creates no personal access token, stored registry credential, payment method, or paid resource.
- A repository-source OCI label links the package before first publication. Package visibility remains a separate control, so anonymous access must be verified and any required visibility change is owner-only. The published package was anonymously readable without a dashboard change, and no agent used a provider dashboard.
- Cloudflare production Workers, Queues, and Workers Analytics Engine are active within the previously validated Free controls. R2 remains unavailable with provider error `10042`, so R2 and backup tasks remain blocked; GHCR-001 changes no Cloudflare resource.
- Existing Supabase staging and production allocations support the deployed application. Browser code uses the publishable-key model and GHCR-001 introduces no Supabase key dependency, project, schema, RLS, function, or quota use.
- Azure for Students status, remaining credit, budget alert, and tooling are not verified. Every Azure task remains blocked and GHCR-001 consumes no student credit.
- HCP Terraform organization, workspaces, and provider credentials are unavailable. Terraform tasks remain blocked and GHCR-001 creates no infrastructure state.
- The active Vercel project continues to serve the non-commercial public portfolio under its existing protected release controls. GHCR-001 changes no Vercel deployment or domain.
- Grafana Cloud Free synthetics and SLOs are active below the recorded allowance. GHCR-001 emits only GitHub workflow metadata and creates no Grafana stream or billable telemetry.
- Open-Meteo use remains non-commercial with attribution in `README.md`; the bootstrap image contains no weather client and creates no provider request.
- Current plan constraints were compared with Section 0.6 and `docs/cost/quota-budget.md`. No failed entitlement authorizes a paid upgrade; unavailable R2, Azure, HCP Terraform, custom-domain, and external-alert lanes remain blocked independently of this scoped GO.
