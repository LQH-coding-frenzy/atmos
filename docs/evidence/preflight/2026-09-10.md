# Provider Preflight 2026-09-10

Result: scoped GO for approved staging database and preview work; later provider tasks remain blocked where noted.

- GitHub repository is public and protected checks are active.
- Cloudflare DNS, Workers Free, and Queues Free are active; the staging Worker and both notification queues are visible.
- Cloudflare Analytics Engine is disabled (`10089`); OBS-007 and release tasks that depend on WAE are blocked until the owner enables it and reconfirms the Free entitlement.
- Cloudflare R2 is not enabled (`10042`), so `CF-IAC-001`, `R2BACK-001`, and dependent backup tasks must not provision or claim R2 readiness without owner approval and a fresh quota check.
- Supabase has two projects. `atmos-staging` (`btrvybyprlcumtovdxsc`) is linked; the owner designated `Weather` (`oxgwprvkotfvyacpqayx`) as production on 2026-09-10.
- Supabase publishable/secret-key modernization is not verified and existing runtime code still references legacy key variable names; production auth/data rollout remains blocked on a separate security task.
- Azure CLI/account and Azure for Students credit/budget status are not available in this environment; Azure tasks remain blocked.
- Terraform/HCP tooling and account plan are not available in this environment; Terraform tasks remain blocked.
- Vercel project access and successful Hobby deployments are verified; use remains limited to the non-commercial portfolio service.
- Grafana Cloud availability and projected synthetic usage are not verified; Grafana/SLO tasks remain blocked.
- Open-Meteo use remains non-commercial, attribution is present, and repository quota guards match the documented free limits.
- No silent paid-tier selection is authorized. Re-run the affected provider checks before each blocked resource family is started.
- The owner approved preview/staging GitHub credentials, both additive staging migrations, versioned staging Edge Function deployment, and production Vercel security headers followed by live gateway data.
