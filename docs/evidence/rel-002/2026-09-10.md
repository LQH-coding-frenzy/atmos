# REL-002 Evidence

- Versioned function names use `api-<12-character-release-id>` derived from the exact protected Git commit.
- Generated wrapper source imports canonical `api-v1`, accepts only its exact versioned URL prefix, and contains no secret value or privileged key name.
- Release metadata is passed per wrapper, so retained functions do not receive a misleading global release value.
- Stable `api-v1`, Worker routing, production Supabase, global secrets, and database schema remain unchanged.
- Deployment occurs only after the implementation is merged and the release ID is derived again on protected `main`.
- Node release and wrapper tests passed: 5 tests, 0 failures.
- Repository typecheck and lint passed.
- Local Edge Runtime candidate smoke returned `200 {"status":"ok"}` and `{"release":"6e4c9fb1be29"}`; retained `api-v1` remained healthy and reported `local`.
- Configured reviewer and security-reviewer agents were unavailable because their model could not be resolved; manual review removed arbitrary release-ID CLI overrides and reconfirmed exact-prefix routing, secret-free source, staging isolation, and stable-function retention.
