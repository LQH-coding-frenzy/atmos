# SLO-001 Evidence

- Dependencies `OBS-002` and `ROUTE-001` are `DONE`. The owner approved four bounded Grafana Cloud Free API synthetics, a zero-percent production function and Worker candidate, the additive dependency-health RPC migration, and direct Grafana API operation with an existing local service-account token on 2026-09-12.
- Scope created no paid feature, payment method, private probe, browser check, alert rule, SLO object, custom domain, new credential, or ordinary candidate traffic.

## Protected implementation

- PR #123 merged the coarse Worker-to-Supabase dependency route as `1163da002bf499c2572682a746d76dcd92ec7faf` after all protected checks passed.
- PR #124 merged supported PostgREST GET handling and sanitized failure logging as `536fd3697b60b81110d56c52451b67fefbd7ef44` after all protected checks passed.
- PR #125 merged `public.atmos_dependency_health()`, publishable-key RPC access, and its ACL tests as `a794b540e4dc83e37675000e06af0979a0e94bc4` after all protected checks passed.
- Production migration `20260912000000_dependency_health_rpc.sql` is additive. The `STABLE SECURITY INVOKER` function executes only `select true`, has an empty `search_path`, revokes execution from `PUBLIC`, `authenticated`, and `service_role`, and directly grants execution only to `anon` besides its owner.
- Immutable Edge Function `api-a794b540e4dc` and Worker candidate `57d8764d-c7e2-41d2-9e46-00c74f17f4c6` passed health, version, dependency, weather, authentication-deny, and exact production-route version-override smoke.
- Production stable Worker `335f0f53-c301-4cc3-bd84-2b9b4e799b9b` remains at 100 percent. Candidate `57d8764d-c7e2-41d2-9e46-00c74f17f4c6` remains at zero percent.

## Grafana checks

All checks are enabled in folder `Atmos` (`fbdzpj`), use public probe `Singapore` (`18`), run every 900,000 ms, require SSL, expect HTTP 200, and have a 10,000 ms timeout.

| ID     | Check                          | Body assertion            | Latest scheduled result |
| ------ | ------------------------------ | ------------------------- | ----------------------- |
| `8192` | `Atmos production frontend`    | none                      | success, HTTP 200       |
| `8193` | `Atmos production edge`        | `"status":"ok"`           | success, HTTP 200       |
| `8194` | `Atmos candidate dependencies` | `"database":"ok"`         | success, HTTP 200       |
| `8198` | `Atmos production weather`     | `"provider":"open-meteo"` | success, HTTP 200       |

- The dependency check sends only the non-secret Cloudflare version override for candidate `57d8764d-c7e2-41d2-9e46-00c74f17f4c6`.
- A direct Prometheus query at `2026-09-12T15:53:52Z` returned `probe_success=1` and `probe_http_status_code=200` for all four checks from Singapore.
- Observed probe durations were 0.269 seconds for frontend, 0.119 seconds for edge health, 0.912 seconds for candidate dependencies, and 0.928 seconds for production weather.
- Grafana's 31-day calculator reports 2,976 executions per check and 11,904 total, 11.904 percent of the 100,000 Cloud Free API allowance and below the 70 percent warning threshold.

## Dashboard

- Dashboard `Atmos Synthetic Monitoring` is active in folder `Atmos` with UID `atmos-synthetics` and six panels.
- `docs/operations/atmos-synthetics-dashboard.json` is the reproducible Classic dashboard API payload and references the live linked Prometheus data source UID `grafanacloud-prom`.
- Grafana accepted the payload through `POST /api/dashboards/db`, stored version 2, and returned `/d/atmos-synthetics/atmos-synthetic-monitoring`.
- The stored resource reads successfully from `dashboard.grafana.app/v2beta1` in namespace `stacks-1827502`. Grafana's server conversion produced six `Panel` elements, `VizConfig`, `QueryGroup`, and `PanelQuery` kinds plus one `QueryVariable`; this avoids unvalidated hand-written V2 types.
- Live Prometheus queries validated availability, `probe_all_success_count` execution counters, HTTP status, probe duration, and TLS expiry. The execution panel deliberately uses the synthetic execution counter rather than counting repeated Prometheus scrape samples.

## Security and rollback

- `GRAFANA_URL` and `GRAFANA_SERVICE_ACCOUNT_TOKEN` were loaded from the ignored local `.env` only inside command processes. The token value was not printed, copied into a file, or committed.
- Grafana API output was filtered to non-sensitive resource metadata, check configuration, and metric results. No authorization header, provider credential, cookie, Supabase key, or database response was recorded.
- Rollback is to disable checks `8192`, `8193`, `8194`, and `8198`, and delete dashboard `atmos-synthetics`. Stable remains at 100 percent, candidate remains at zero percent, and the additive inert RPC can remain for forward compatibility.
