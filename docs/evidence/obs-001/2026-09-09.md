# OBS-001 Evidence

- Worker and Supabase Edge Function boundaries preserve bounded request IDs and valid W3C `traceparent` values.
- Missing, oversized, malformed, or all-zero correlation values are replaced before they reach response headers, logs, or downstream services.
- Gateway error envelopes include the normalized request ID.
- Worker-to-function, Edge Function-to-Worker, and queue-consumer-to-function calls propagate both correlation headers.
- Worker and Edge Function CORS middleware allow browser `traceparent` propagation from configured origins.
- Sanitized weather-provider logs include `request_id` and `trace_id` without JWTs, cookies, user data, or request bodies.
- No remote deployment, vendor export, durable state, or schema change is included.
- A cross-runtime contract test locks the independently bundled Worker and Edge Function normalizers to identical behavior.
- The configured security-reviewer was unavailable because its model could not be resolved; manual review found no additional injection, propagation, or authorization issue.
- Fifteen focused tests, root typecheck/lint, Worker dry-run bundle, formatting, and diff checks passed.
- Local Supabase Edge Runtime smoke tests returned `200`, preserved valid correlation headers, and replaced oversized/all-zero attacker-controlled values.
- Protected PR #93 passed Vercel, quality, database, Playwright, SBOM, CodeQL, Gitleaks, Semgrep, and Trivy checks before merge.
