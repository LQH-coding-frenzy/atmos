# OBS-007 Evidence

- Dependencies `EDGE-002` and `REL-001` are `DONE` in `.agent/status.yaml`.
- Production and staging datasets are isolated by configuration; this task deploys only `atmos_worker_requests_staging` on `atmos-gateway-staging`.
- The positional dataset contract contains only bounded release, route-group, status-class, cache, backend, provider, and duration values.
- Raw paths, URLs, query strings, headers, credentials, request IDs, user identifiers, and other personal data are excluded.
- Analytics write exceptions are contained and emit only a fixed event name and error class.
- Existing staging secret names `INTERNAL_QUEUE_SECRET` and `SUPABASE_FUNCTION_URL` were confirmed before implementation; no secret value was read or changed.
- Focused request analytics and gateway tests passed: 19 tests, 0 failures.
- Repository typecheck and lint passed; owned files pass formatting and diff checks.
- Explicit staging Wrangler dry-run succeeded with Queue, `atmos_worker_requests_staging`, Worker version metadata, and CORS bindings.
- Configured reviewer and security-reviewer agents were unavailable because their model could not be resolved; manual review reconfirmed bounded dimensions, canonical release validation, staging/production isolation, contained write failures, and no user-controlled raw value in the dataset.
- Protected implementation PR #105 passed Vercel, quality, database, Playwright, SBOM, CodeQL, Gitleaks, Semgrep, and Trivy checks before merge as `8ad1a3790aa63e7e9b86f9bdfc21603d13f79c1f`.
- The staging upload was rejected before version creation with Cloudflare API code `10089`: Analytics Engine is disabled for account `50f15456a2d4abf3186c504f406da3fe`.
- Post-failure verification confirmed the active staging deployment is still version `1b680425-d000-4ecb-8101-f9c6c9c2ea9f`, both existing secret names remain present, and `/health` plus `/version` still return 200.
- OBS-007 was blocked until the owner enabled Analytics Engine, confirmed it remained within the Free entitlement, and authorized a retry.
- The owner authorized enabling Analytics Engine only on its Free entitlement. Current Cloudflare documentation confirmed the Workers Free daily allowance is 100,000 written data points and 10,000 read queries; the dashboard enabled Analytics Engine without a plan upgrade.
- Staging release `113096e10043` uploaded successfully with version ID `b3077c30-0e42-4d60-95a7-264754d126c6`, but post-deploy smoke exposed an existing proxy integration defect: the Worker route returned 500 instead of the direct Supabase response of 401.
- Staging was immediately rolled back to `1b680425-d000-4ecb-8101-f9c6c9c2ea9f`; `/health`, `/version`, and the unauthenticated proxy returned 200, 200, and 401 after rollback.
- Diagnostic preview versions proved that the WAE binding and analytics middleware were not the cause. The prior proxy form returned 401; timing/no-filter variants returned 500; cloning the upstream fetch response into a mutable `Response` restored 401 while preserving status, body, and headers for Hono middleware.
- The correction also forwards only application `Accept`, `Authorization`, and `Content-Type` headers, adds validated correlation headers, and strips cookies plus Cloudflare/client-network metadata before the Supabase hop. A direct spoofed `CF-Connecting-IP` diagnostic returned 403, reinforcing that it must not be inherited.
- Configured reviewer and security-reviewer retries remained unavailable; manual review confirmed method/body/auth preservation, mutable response handling, and a single immutable header allowlist outside the request path.
- Protected correction PRs #107 and #108 passed Vercel, quality, database, Playwright, SBOM, CodeQL, Gitleaks, Semgrep, and Trivy checks before merge.
- Protected release `b67be04c5f67` deployed only to `atmos-gateway-staging` as Worker version `84711379-0d86-4e62-886c-68d5fe2c0c18`; retained rollback version `1b680425-d000-4ecb-8101-f9c6c9c2ea9f` was not deleted.
- Active staging `/health` and `/version` returned 200 with valid request/trace correlation; `/version` reported exactly `b67be04c5f67`.
- Active staging unauthenticated proxy and notification publish smokes each returned 401; repeated cached weather requests returned 200 with `x-cache: HIT`.
- A bounded 200-request, 10 requests/second cached-weather run returned 200 for every request. Cloudflare tail sampled 199 invocations: CPU p50 `1 ms`, p95 `1 ms`, p99 `7 ms`, and max `10 ms`. The p95 passes the 7 ms placement gate.
- Cloudflare Analytics Engine listed `atmos_worker_requests_staging` with 381 data points after the smokes and CPU run, proving that the configured binding receives request telemetry.
