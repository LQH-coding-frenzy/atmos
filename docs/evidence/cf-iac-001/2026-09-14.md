# CF-IAC-001 Evidence

- PR #157 established the Cloudflare Terraform modules; PR #158 corrected the API to a Worker custom domain.
- HCP Terraform created `atmos-production-backups` in APAC and adopted existing `atmos-notifications` and `atmos-notifications-dlq` queues into state.
- HCP apply replaced the failing Workers.dev CNAME with custom-domain ID `efcc298f7f2372656b27f470f3bf3703649b606f` for service `atmos-gateway`.
- `https://api.rainify.dpdns.org/health` returned HTTP 200 with `{"status":"ok"}` after apply.
