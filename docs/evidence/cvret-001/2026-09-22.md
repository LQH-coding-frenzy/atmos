# CVRET-001 Retirement Evidence

Result: complete. The approved queue, alert-delivery, and unused Azure container-platform
runtime were removed without changing Supabase schema, RLS policies, or the R2 backup archive.

## Approval And Release

- The owner-approved scope is recorded in `docs/evidence/preflight/2026-09-21.md`.
- Protected retirement workflow run
  [`35633952292`](https://github.com/LQH-coding-frenzy/atmos/actions/runs/35633952292)
  completed successfully from `main` SHA `443821846676478c4ebc131875f3598b67538727`.
- The run deployed queue-free Worker and Supabase function releases, detached queue consumers,
  removed staging queues, removed retired Worker secret names, and restored the tagged
  production Worker release.
- Protected Vercel production release run
  [`35708932826`](https://github.com/LQH-coding-frenzy/atmos/actions/runs/35708932826)
  successfully built, smoke-tested, and promoted the exact `443821846676` dashboard artifact.

## Provider Retirement

- HCP Terraform edge apply
  [`run-vh1JqtARXv2ZiikC`](https://app.terraform.io/app/atmos_uit/atmos-edge-production/runs/run-vh1JqtARXv2ZiikC)
  destroyed only `cloudflare_queue.notifications` and
  `cloudflare_queue.notifications_dlq`.
- HCP Terraform Azure apply
  [`run-gYgLRJiHsZ1fVrWs`](https://app.terraform.io/app/atmos_uit/atmos-azure-production/runs/run-gYgLRJiHsZ1fVrWs)
  deleted the Container Apps environment. AzureRM polling returned an error after that deletion;
  the follow-up refresh detected the environment as absent and planned only the remaining
  Log Analytics workspace.
- HCP Terraform Azure follow-up apply
  [`run-RM1S5xa8y611dpoW`](https://app.terraform.io/app/atmos_uit/atmos-azure-production/runs/run-RM1S5xa8y611dpoW)
  destroyed only `azurerm_log_analytics_workspace.production` successfully.
- Final HCP Terraform plans reported no changes:
  [`run-K2cHFkUyuwtTo1FB`](https://app.terraform.io/app/atmos_uit/atmos-edge-production/runs/run-K2cHFkUyuwtTo1FB)
  retains the Worker custom domain and `atmos-production-backups`;
  [`run-p5VGqirax8vjaYKc`](https://app.terraform.io/app/atmos_uit/atmos-azure-production/runs/run-p5VGqirax8vjaYKc)
  retains only `azurerm_container_group.backup`.

## Final Checks

- `https://api.rainify.dpdns.org/version` returned release `443821846676`.
- `https://api.rainify.dpdns.org/health/dependencies` returned HTTP 200 with healthy database status.
- The public Berlin dashboard weather route returned a valid Open-Meteo response.
- `POST /internal/notifications/publish` returned HTTP 404.
- Cloudflare Queue inventory was empty; the production Worker secret inventory contained only
  `SUPABASE_FUNCTION_URL`.
- `https://rainify.dpdns.org/` rendered live weather data with no browser-console errors.
- Historical task validation passed lint, typecheck, unit/contract tests, local database/RLS tests,
  Worker build, web build, browser tests, runbook validation, security checks, and targeted formatting.

## Retained Evidence And Recovery Boundary

- The Supabase schema, RLS policies, application data, Worker custom API domain, encrypted R2 archive,
  and backup Container Instance remain intact.
- Recreating the retired queues or Azure resources requires a new approved provision; the safe recovery
  path for the active showcase is redeploying the queue-free release through protected workflows.
