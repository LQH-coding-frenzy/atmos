# CI-003 Browser Runner Remediation Evidence

- GAME-001 PR #95 reached no application or browser test code because the Google Chrome apt repository repeatedly returned a package-index hash mismatch.
- Atmos installs Playwright's bundled Chromium, so the runner image's unrelated `google-chrome*` apt source files are removed only from the ephemeral runner before `playwright install --with-deps chromium`.
- Playwright still installs and validates all required operating-system dependencies; no test, assertion, browser version, or required check is weakened.
- No application, persistent runner, credential, or production state changes.
- Local Playwright smoke passed 1/1; the protected PR must prove dependency installation on `ubuntu-24.04`.
- The configured security-reviewer was unavailable because its model could not be resolved; manual review confirmed the runner-local change preserves `--with-deps` and all browser assertions.
- Protected PR #96 proved the corrected `ubuntu-24.04` dependency installation and passed Playwright, Vercel, quality, database, SBOM, CodeQL, Gitleaks, Semgrep, and Trivy before merge.
