# HIST-001 Evidence

- `corepack pnpm exec supabase db reset` reconstructs the local schema and applies `20260905000000_weather_snapshots.sql` after the immutable base migration.
- `corepack pnpm test:db` passes 35 pgTAP assertions: 20 baseline assertions and 15 snapshot schema/RLS assertions.
- Snapshot coverage verifies owner select and insert allow rules, cross-user select/insert/delete denial, explicit authenticated `SELECT`/`INSERT`/`DELETE` grants, no authenticated `UPDATE` grant, and all three ownership policies.
- The initial local test caught a default authenticated `UPDATE` privilege; the migration explicitly revokes it. No existing policy was weakened.
- No remote database apply, provider call, secret-key use, destructive schema operation, or paid-resource change occurred.
