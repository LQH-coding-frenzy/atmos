# REL-004 Evidence

- Dependencies `REL-003`, `OBS-008`, and `SLO-001` are `DONE`. The owner explicitly approved continuing through the fully live production service, including progressive production traffic changes, on 2026-09-12.
- Scope created no paid resource, credential, custom domain, database change, Worker version, or Edge Function version. Existing stable and candidate releases remained available throughout the rollout.

## Protected release gates

- PR #127 added the production-dataset WAE choice and freshness-aware Grafana synthetic gate. Quality, database, Playwright, CodeQL, Gitleaks, Semgrep, Trivy, SBOM, and Vercel checks passed before merge as `cfbe7dd`.
- The WAE workflow retains `contents: read`, accepts only `atmos_worker_requests_staging` or `atmos_worker_requests`, and uses the existing protected Account Analytics Read token. It has no deployment permission.
- The Grafana gate requires recent successful `probe_success` samples for exactly four Atmos checks and returns `FAIL` or `INSUFFICIENT_DATA` for failing, missing, stale, malformed, or future-dated telemetry.
- Local gate tests passed 12 assertions. ESLint and repository typecheck passed. The live Grafana gate returned `PASS` for all four checks before and throughout promotion.

## Release identities

- Previous stable Worker: `335f0f53-c301-4cc3-bd84-2b9b4e799b9b`, release `763e8c651218`.
- Promoted Worker: `57d8764d-c7e2-41d2-9e46-00c74f17f4c6`, release `a794b540e4dc`.
- Promoted Worker references immutable Supabase Edge Function `api-a794b540e4dc`.

## Progressive rollout

All WAE gates queried `atmos_worker_requests` with a 60-minute lookback and minimum 30 weighted samples per exact release. Controlled health requests supplemented low organic traffic and are not represented as user traffic.

| Stage        | Deployment ID                          | Bounded observation                          | WAE workflow  | Stable samples | Candidate samples | Errors | Decision |
| ------------ | -------------------------------------- | -------------------------------------------- | ------------- | -------------- | ----------------- | ------ | -------- |
| Pre-exposure | existing 100/0 deployment              | exact-version smoke                          | `34706399317` | 49             | 45                | 0%/0%  | `PASS`   |
| 10%          | `7ee519c4-e73b-4be0-ad57-5bd3635b6510` | 92 stable / 8 candidate of 100               | `34706672233` | 152            | 74                | 0%/0%  | `PASS`   |
| 25%          | `b57854b2-d054-430b-933b-eafb32977ff9` | 74 stable / 26 candidate of 100              | `34706816626` | 225            | 130               | 0%/0%  | `PASS`   |
| 50%          | `7acdef4c-31fb-408f-8419-65a83a6fe0f4` | deployment inventory plus controlled samples | `34706968009` | 357            | 265               | 0%/0%  | `PASS`   |
| 100%         | `572dd0e1-6de5-46f9-b2f1-5454aaf3e414` | 50 candidate / 0 previous of 50              | `34707184154` | 401            | 415               | 0%/0%  | `PASS`   |

Candidate p95 was 786 ms at pre-exposure and 784 ms at 10 percent, below the 2,500 ms gate; later controlled health samples rounded weighted p95 to 0 ms for both releases.

## Final production state

- Deployment `572dd0e1-6de5-46f9-b2f1-5454aaf3e414` assigns 100 percent to `57d8764d-c7e2-41d2-9e46-00c74f17f4c6` and keeps `335f0f53-c301-4cc3-bd84-2b9b4e799b9b` attached at zero percent for rollback.
- Fifty ordinary `/version` requests all returned release `a794b540e4dc`.
- Ordinary `/health`, `/version`, `/health/dependencies`, and public weather smoke passed after full promotion.
- Grafana checks `8192`, `8193`, `8194`, and `8198` remained enabled and successful. Check `8194` was converted after promotion from an exact candidate override to the ordinary `Atmos production dependencies` monitor.
- Worker source is unchanged from the OBS-007 release measured at p95 CPU 1 ms, below the 7 ms budget.

## Security and rollback

- Cloudflare and Grafana credentials remained in their existing protected environments or ignored local `.env`; values were not printed, copied, or committed.
- Bounded release traffic remained far below Workers Free request limits and synthetic configuration remained at 11,904 projected monthly executions.
- Immediate rollback remains an explicit stable-only deployment of `335f0f53-c301-4cc3-bd84-2b9b4e799b9b` at 100 percent. Both Supabase function releases and additive database schema remain retained. REL-006 owns automated rollback and REL-007 owns safe cleanup.
