# REL-003 Evidence

- Dependencies `REL-002` and `ROUTE-001` are `DONE`. The owner approved the Free-tier production bootstrap, additive migration apply, generated internal secret, first stable Worker, and zero-percent candidate smoke on 2026-09-12.
- Scope excluded paid resources, destructive or contract migrations, custom DNS, frontend switching, stable cleanup, and ordinary candidate traffic.

## Protected changes

- PR #119 passed Vercel, quality, database, Playwright, CodeQL, Gitleaks, Semgrep, Trivy, and SBOM checks before merging as `763e8c6512183493d047d893801e4ca259ee95b4`. It isolated staging queue bindings and added the production canary contract/runbook.
- Production function smoke then found that canonical `api-v1` did not inject Deno environment bindings into Hono, so its CORS response failed closed before any production Worker existed.
- PR #120 injected `CORS_ORIGIN` and `RELEASE_ID` at the canonical entrypoint. It passed the same protected checks before merging as `43ba871555322979dc8d61fed412832871f826ba`.

## Queue isolation

- Created Free queues `atmos-notifications-staging` and `atmos-notifications-staging-dlq`.
- Staging version `749b20db-3def-4ce4-91d3-983ec0b6d345`, tagged `763e8c651218`, passed preview smoke and was deployed at 100 percent. The previous version `e727993c-fad4-4caf-94e8-53aacc56eb48` remains available for rollback.
- Cloudflare treats queue consumers as non-versioned triggers. After `wrangler triggers deploy --env staging`, the stale `atmos-gateway-staging` consumer was explicitly removed from `atmos-notifications`.
- Final inventory reports one producer and one consumer on `atmos-notifications-staging`, one producer and one consumer on `atmos-notifications`, and no consumer on either DLQ.

## Production database and functions

- Initial production migration history was empty. The dry-run listed exactly the seven tracked migrations from `20260901000000` through `20260908000000`.
- Review confirmed only schema creation, indexes, explicit grants/RLS policies, a user-creation trigger, and nullable coordinate expansion. PR #119's database check passed all 107 pgTAP assertions, including cross-user deny cases.
- The approved apply completed all seven migrations. Post-apply history matches local exactly and a second dry-run reports the remote database is up to date.
- Production secret inventory confirms `CORS_ORIGIN`, `GATEWAY_URL`, and `INTERNAL_QUEUE_SECRET` by name only; no credential value was read or recorded. `GATEWAY_URL` points reconciliation to the public production Worker route.
- Canonical `api-v1` is active at version 2 with function ID `7110868f-ee9e-4211-84a8-924d10608ce8`.
- Candidate `api-43ba87155532` is active at version 1 with function ID `a83554ba-1192-42fa-8602-a6f5fd1749d1`. Superseded `api-763e8c651218` is retained unreferenced for REL-007 cleanup rather than deleted during the rollback window.
- Stable and candidate function smoke returned health 200, expected version metadata (`local` for canonical and `43ba87155532` for immutable), `/api/v1/me` 401, internal delivery 401, and exact production CORS.

## Production Worker proof

- Initial deployment inventory returned Cloudflare code `10007` because `atmos-gateway` did not exist. Wrangler then rejected first-version upload and required a first `wrangler deploy`.
- Stable Worker version `335f0f53-c301-4cc3-bd84-2b9b4e799b9b`, tagged `763e8c651218`, was created and deployed at 100 percent at `https://atmos-gateway.rainify.workers.dev`. It references canonical `api-v1`.
- Candidate Worker version `6d476f13-c565-4ba1-b0f4-69ce13711f45`, tagged `43ba87155532`, references immutable `api-43ba87155532`.
- Final deployment status is stable 100 percent and candidate 0 percent. An ordinary `/version` request returned stable `763e8c651218`.
- A structured `Cloudflare-Workers-Version-Overrides` request through the production route returned candidate `43ba87155532`. Candidate health and weather returned 200; internal publish and `/api/v1/me` returned 401; CORS and correlation headers were present.
- Worker runtime source and its shared contracts/providers are unchanged from the OBS-007 release measured at p95 CPU 1 ms, below the 7 ms budget.
- Worker secret inventory contains only the expected names `INTERNAL_QUEUE_SECRET` and `SUPABASE_FUNCTION_URL`. The three ephemeral local secret files were deleted and their absence was verified after both uploads.

## Validation and rollback

- Focused gateway tests: 19 passed. Release/versioned-function tests: 5 passed. Typecheck, lint, formatting, production/staging bundle dry-runs, function smoke, Worker preview smoke, stable smoke, and production version-override smoke passed.
- The configured security-review agent was unavailable because its model could not be resolved. Bounded manual review of route authorization, RLS coverage, secret handling, queue isolation, CORS, and rollback found the missing `GATEWAY_URL`; it was set and its name verified before finalization. No remaining finding blocks REL-003.
- Rollback remains stable Worker `335f0f53-c301-4cc3-bd84-2b9b4e799b9b` alone at 100 percent. Additive schema and both referenced function releases remain retained; REL-007 owns cleanup.
