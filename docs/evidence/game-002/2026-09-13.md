# GAME-002 Evidence

## Scope And Safety

- Dependencies `REL-006`, `OBS-006`, `OBS-008`, and `SLO-001` were `DONE` before the exercise.
- The owner approved the bounded production game day on 2026-09-13. Candidate exposure never exceeded 10 percent, no database or RLS change occurred, and no secret was created or rotated.
- Stable Worker `57d8764d-c7e2-41d2-9e46-00c74f17f4c6` remained mapped to release `a794b540e4dc` and Supabase function `api-a794b540e4dc`.
- Exercised candidate Worker `5ed169c2-8263-42d3-bde7-8a9148d4f2ff` was mapped to release `2de69093b70d` and active function `api-2de69093b70d`.
- Unused Worker upload `73813673-c218-4f42-9bc7-cd9c12a2a258` also has tag `2de69093b70d`, inherited the previous backend URL, and was never deployed. It is not the exercised candidate.

## Zero-Percent Validation

- PR #137 merged the deployment-only fault hook as `2de6909` after protected checks passed.
- Production function `api-2de69093b70d` returned health HTTP 200 and exact release `2de69093b70d`.
- Deployment `2f12b184-0e71-4342-bee6-25fc27e07050` held stable at 100 percent and candidate at zero percent.
- Candidate override smoke passed health, dependency health, release identity, and unauthenticated HTTP 401 checks. Candidate weather returned the intended sanitized HTTP 503 `WEATHER_UNAVAILABLE`; ordinary stable weather remained HTTP 200.
- Forty stable and forty candidate override weather samples were generated while candidate traffic remained at zero percent. The controlled failing branch did not call Open-Meteo.
- WAE run `34753803679` returned the expected `FAIL` before ordinary exposure: stable had 54 samples, 0 error rate, and 945 ms p95 wall duration; candidate had 45 samples, 0.9111111111111111 error rate, and 0 ms p95 wall duration. The allowed error rate was 0.02.

## First Exposure And Recovery

- Deployment `a9c24e9c-142b-4e9a-bc48-08fb16d63fba` exposed the candidate at 10 percent from `2026-09-13T11:13:35Z` until emergency rollback deployment `803b4e4a-e699-4986-8aab-ff87f9257333` at `2026-09-13T11:16:50Z`, a bounded exposure of 3 minutes 15 seconds.
- WAE run `34753896861` returned the expected deterministic `FAIL` at 10 percent.
- Initial protected rollback run `34753940171` passed planning and stable-target smoke but failed deployment with Cloudflare code `10220`. A one-version `versions deploy` cannot restore a prior Worker after `SUPABASE_FUNCTION_URL` changed because it does not restore the target version's prior versioned bindings.
- The documented emergency `wrangler rollback --yes` path restored stable-only deployment `803b4e4a-e699-4986-8aab-ff87f9257333`; health, dependency health, release identity, weather, and authorization smoke recovered.
- PR #138 replaced the protected workflow's deployment operation with Cloudflare's dedicated `wrangler rollback` primitive and added regression coverage. It merged as `2e9ab6c` after protected checks passed.

## Corrected Protected Rollback

- Deployment `b46934b6-cc69-4c01-b8fe-03e3cf31f3ff` exposed stable at 90 percent and candidate at 10 percent from `2026-09-13T11:23:50Z` until protected rollback deployment `e5d82173-6362-45d3-9fd2-5e88ab465b57` at `2026-09-13T11:27:35Z`, a bounded exposure of 3 minutes 45 seconds.
- The deployment's provider message is accidentally `GAME-nub?/not`. The immutable deployment ID, traffic split, Worker identities, gate run, and rollback run are authoritative; this record does not rewrite provider history.
- WAE run `34754362450` returned expected `FAIL`: stable had 63 samples, 0 error rate, and 874 ms p95 wall duration; candidate had 45 samples, 0.9111111111111111 error rate, and 0 ms p95 wall duration. The allowed error rate was 0.02.
- Protected rollback run `34754470476` passed plan, retained-target smoke, rollback, deployment verification, and recovered-path smoke from exact protected SHA `2e9ab6c3c0a1d92bcaa61736c784bc8fec75a25e`.
- The rollback job is recorded at `https://github.com/LQH-coding-frenzy/atmos/actions/runs/34754470476/job/103716402620`.
- Final deployment `e5d82173-6362-45d3-9fd2-5e88ab465b57` serves stable Worker `57d8764d-c7e2-41d2-9e46-00c74f17f4c6` at 100 percent with no candidate traffic.
- Final probes returned `/health` HTTP 200 with `status=ok`, `/health/dependencies` HTTP 200 with database `ok`, `/version` release `a794b540e4dc`, public weather HTTP 200, and unauthenticated `/api/v1/me` HTTP 401.
- The Grafana synthetic gate returned `PASS`; frontend, edge, dependency, and weather probes were fresh and successful.

## Source Retirement And Retention

- The completion change removes `GAME_DAY_FAILURE_MODE`, its weather-handler branch, and its dedicated test. A future game day must reintroduce a deployment-only hook through a new protected PR.
- Failed candidate artifacts and `api-2de69093b70d` remain retained through `DOC-002` review. Production was not redeployed solely to remove the hook because the current stable Worker never contained it.
- No token, cookie, JWT, credential value, user payload, or provider response body is recorded in this evidence.
- Twenty-two focused gateway and request-analytics tests passed. Gateway dry-run bundle, root lint, root typecheck, runbook validation, task-file formatting, and `git diff --check` also passed.
- The repository-wide formatting check was blocked only by 148 pre-existing untracked `.playwright-mcp` snapshots that are outside task scope and preserved unchanged. All six canonical GAME-002 completion files pass Prettier directly.
- The configured reviewer was unavailable because its model could not be resolved. Manual review found no remaining executable fault hook, loss of normal sanitized provider-failure coverage, evidence inconsistency, or rollback impairment.
