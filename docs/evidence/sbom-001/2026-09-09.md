# SBOM-001 Evidence

- The protected security workflow generates a CycloneDX JSON inventory with Trivy for the exact checked-out commit.
- CI fails when the SBOM is missing, invalid JSON, not CycloneDX, or contains no components.
- The artifact name includes `github.sha` and retention is bounded to 14 days.
- Checkout, Trivy, Node setup, and artifact upload actions are pinned to full commit SHAs; the job has read-only repository permission and no secrets.
- Container-image SBOM, signing, attestations, and release attachment remain outside SBOM-001.
- Local formatting and whitespace validation passed; Trivy execution and artifact inspection are required on the protected PR because Trivy is not installed locally.
- The configured security-reviewer was unavailable because its model could not be resolved; a manual bounded workflow review found no additional issue.
