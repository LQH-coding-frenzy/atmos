# PREVIEW-002 Evidence

- Dependencies `CI-001` and `EDGE-001` are `DONE`.
- Scope is limited to one inactive `atmos-gateway-staging` Worker version on Workers Free; no production route, traffic deployment, database, Terraform, paid resource, or new credential is involved.
- `preview_urls` is explicit for the base and staging configurations so preview behavior does not depend on Wrangler defaults.
- Preview aliases and version tags are derived from the protected 12-character Git release ID; the upload uses `--strict` and the existing local Wrangler OAuth session.
- Implementation PR #113 passed Vercel, quality, database, Playwright, SBOM, CodeQL, Gitleaks, Semgrep, and Trivy checks before merging as `fd0070486507506a1deb7341208bb383fe719374`.
- Version `fc3ce391-f2dc-4fcb-a0ae-5faedf4e264d` proved that the version metadata tag is not exposed to this preview execution path: `/version` returned `local`. The alias was repointed to a corrected upload with explicit non-secret `RELEASE_ID`; the failed version was never deployed. Current Wrangler and dashboard controls do not offer single-version deletion.
- Corrected inactive version `c60227d6-8b59-4731-922f-c086d8d49cbf` is tagged `fd0070486507` and available at `https://c60227d6-atmos-gateway-staging.rainify.workers.dev` with alias `https://preview-fd0070486507-atmos-gateway-staging.rainify.workers.dev`.
- Exact preview smoke returned health 200, version 200 with release `fd0070486507`, weather 200, internal publish 401, and `/api/v1/me` 401. No credential or response body was recorded.
- Before and after upload, active staging traffic remained 100% on version `e727993c-fad4-4caf-94e8-53aacc56eb48`, tagged `14434e26b79b`; no deployment or route changed.
- Worker source and runtime behavior were unchanged, so the OBS-007 measured p95 CPU of 1 ms remains applicable; preview URLs do not expose normal Worker analytics.
