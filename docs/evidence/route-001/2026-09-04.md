# ROUTE-001 Evidence

- The Worker forwards `/api/*` requests only when `SUPABASE_FUNCTION_URL` is explicitly configured.
- The proxy preserves query parameters and propagates the request ID; absent configuration returns a sanitized `503`.
- Focused gateway tests pass and Worker dry-run validates the bundle.
- No remote Worker, function, or database change occurred.
