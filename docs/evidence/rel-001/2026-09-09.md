# REL-001 Evidence

- Release IDs are the first 12 lowercase hexadecimal characters of a validated full Git SHA.
- GitHub metadata takes precedence over Vercel metadata; local execution resolves the exact repository `HEAD`.
- Partial or malformed source metadata fails closed.
- ESLint applies Node globals to release scripts and excludes generated local Vercel output without suppressing repository source.
- Runtime propagation and remote deployment remain owned by dependent release tasks.
- Three Node tests, local CLI resolution, root lint, root typecheck, formatting, and diff checks passed.
- The configured reviewer was unavailable because its model could not be resolved; a manual bounded review found no additional issue.
