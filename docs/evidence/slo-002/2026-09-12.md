# SLO-002 Evidence

- Dependency `SLO-001` is `DONE`. The owner authorized continuing to a fully live production service on 2026-09-12.
- Grafana Cloud SLO plugin `1.98.0` is enabled. The stack reports `usesDatasourceRecordingRules: false`, so Grafana-managed rules own SLO evaluation.
- No paid feature, credential, contact point, notification policy, browser synthetic, application change, or database change was created.

## Protected configuration

- PR #129 defined the event-based SLO payload and operating procedure. Quality, database, Playwright, CodeQL, Gitleaks, Semgrep, Trivy, SBOM, and Vercel checks passed before merge as `c85ef80`.
- `docs/operations/atmos-slos.json` is the reproducible API payload. It uses the existing `grafanacloud-prom` source and destination data source.
- Availability uses `probe_all_success_sum` divided by `probe_all_success_count`; latency uses the `le="2.5"` duration bucket divided by the duration count. Both are synthetic execution counters rather than repeated Prometheus scrape samples.

## Active SLOs

| SLO                                 | UUID                    |                   Objective |  Window |
| ----------------------------------- | ----------------------- | --------------------------: | ------: |
| Atmos public weather availability   | `jcmsyyihfk6uhgi19d5m7` |                       99.5% | 28 days |
| Atmos public weather client latency | `evgiwsqi2ipq6tgr2p4nm` | 95% at or below 2.5 seconds | 28 days |

- Both SLO API dashboards returned HTTP 200.
- At `2026-09-12T18:18:34Z`, direct one-hour source queries returned availability ratio `1.0` and under-2.5-second latency ratio `1.0`.
- Each SLO generated 15 Grafana-managed rules: 13 recording/objective rules, one critical fast-burn alert, and one warning slow-burn alert.
- All 30 generated rules reported health `ok`; all four burn alerts were inactive. Alert rules evaluate every 60 seconds and require two minutes before firing.
- Generated burn alerts use `noDataState: OK` and `execErrState: KeepLast`. Missing source data is covered independently by the freshness-aware SLO-001 synthetic release gate and must still be investigated rather than interpreted as service health.

## Notification boundary

- Grafana's default notification policy currently has receiver `empty`; the only existing contact point is the provider-owned insight processor webhook.
- SLO-002 creates alert evaluation and in-Grafana visibility only. It does not invent an email or webhook destination. External notification routing remains blocked until the owner supplies or approves a receiver.

## Security and rollback

- `GRAFANA_URL` and `GRAFANA_SERVICE_ACCOUNT_TOKEN` were loaded from ignored local `.env` only inside command processes. No token value was printed, copied, or committed.
- Rollback deletes only SLO UUIDs `jcmsyyihfk6uhgi19d5m7` and `evgiwsqi2ipq6tgr2p4nm`, then verifies their 30 generated rules are removed. The shared Prometheus data source and four production synthetics remain intact.
