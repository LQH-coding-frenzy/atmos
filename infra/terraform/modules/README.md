# Terraform Modules

Owning tasks add resources only to these modules: `cloudflare-edge`, `cloudflare-r2`, `vercel-project`, `supabase-platform`, `azure-container-apps`, `azure-identity`, and `observability`.
